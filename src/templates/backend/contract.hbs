// SPDX-License-Identifier: MIT
pragma solidity ^0.8.4;
{{#if openzeppelinConfirmation}}

import "@openzeppelin/contracts/token/{{tokenType}}/{{tokenType}}.sol";
import "@openzeppelin/contracts/access/Ownable.sol";

contract {{pascalCase contract}} is {{tokenType}}, Ownable {
    constructor() {{tokenType}}("{{tokenName}}", "{{tokenSymbol}}") {

    }
}   
{{else}}

contract {{pascalCase contract}} {

} 
{{/if}}
