const { ethers } = require("hardhat");

async function main() {
  /*
  A ContractFactory in ethers.js is an abstraction used to deploy new smart contracts,
  so {{camelCase contract}} here is a factory for instances of our Whitelist contract.
  */
  const {{camelCase contract}}Contract = await ethers.getContractFactory("{{pascalCase contract}}");

  // here we deploy the contract
  const deployed{{pascalCase contract}}Contract = await {{camelCase contract}}Contract.deploy();
  
  // Wait for it to finish deploying
  await deployed{{pascalCase contract}}Contract.deployed();

  // print the address of the deployed contract
  console.log(
    "{{pascalCase contract}} Contract Address:",
    deployed{{pascalCase contract}}Contract.address
  );
}

// Call the main function and catch if there is any error
main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  })
