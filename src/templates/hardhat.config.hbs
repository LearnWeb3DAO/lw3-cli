require("@nomiclabs/hardhat-waffle");
require("dotenv").config({ path: ".env" });

const ALCHEMY_API_KEY_URL = process.env.ALCHEMY_API_KEY_URL;

const {{upperCase network}}_PRIVATE_KEY = process.env.{{upperCase network}}_PRIVATE_KEY;

module.exports = {
  solidity: "0.8.4",
  networks: {
    {{lowerCase network}}: {
      url: ALCHEMY_API_KEY_URL,
      accounts: [{{upperCase network}}_PRIVATE_KEY],
    },
  },
};